// Esto es un comentario. los comentatios no se compilan
// Esto es otro comentario.
// Los comentario sirven para a;adir anotaciones
// Se utilizan para que el codigo sea mas legible

/* Esto es un comentario
en varias lineas
funciona igual
 */

// Declaracion de librerias "include"
#include <stdio.h> //libreria estandar intrada y salida header
#include <math.h> //Libreria funciones matematicas

/* Todo programa tiene funicion main int tipo dato entero, cuando un codigo termina regresa un dato 
()si se le para alguntipo de argumento, si estan vacios que no se le para nada.
 */
// inicio funcion principal
int main (){
    int numero_entero=40; //Toda declaracion se realiza al comienzo 
    float numeroDecimal=3.14;
    float resultado;
    float resultado1=2, resultado2, resultado3;

    // zona de trabajo
    printf("HOLA MUNDO");

} //final de la funcion principal






/* Tipos de datos
400 -> numero entero (integer)
3,14 -> numero decimal (Float)
40000000000000000000000 -> numero entero longitud(double)
h -> caracter (char)
hola -> palabra (string) array (coleccion)de caracteres
verdadero falso (bool) -> true false (boolean)

funciones
*/