#include <stdio.h>

int main (){
    int num=10, num2=20;
    float num3=3.45;
    printf(" El valor de la variable es: %d y %d \n",num,num2);
    // las variables pueden ser llamadas en cualquier momento
    printf("Muetro la variable num2: %d \n",num2);
    printf("La variable 3 es decimal num3: %.2f",num3);
}